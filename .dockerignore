# Git
.git
.gitignore

# Node modules
node_modules
npm-debug.log
yarn-error.log

# Build output (will be copied from builder stage)
dist

# Environment
.env
.env.*

# Database (exclude temporary files but keep nodes.db for Docker image)
data/*.db-shm
data/*.db-wal
# Allow nodes.db to be copied into Docker image
!data/nodes.db
!data/nodes-sample.json

# IDE
.vscode
.idea

# OS
.DS_Store
Thumbs.db

# Documentation (not needed in image)
*.md
!README.md

# Docker
docker-compose.yml
.dockerignore
